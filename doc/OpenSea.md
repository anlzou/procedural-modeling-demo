# Open Sea — Realtime WebGPU Ocean

> **实时程序化海洋渲染** — 基于 WebGPU 与 Three.js TSL 节点着色器，无需任何纹理贴图，纯数学驱动的海洋模拟。

---

## 📖 项目来源

本项目最初由 Kimi Agent 集群生成，是一个展示多 Agent 并行处理复杂任务的示例。

- [Kimi Agent 集群 | 多 Agent 并行处理复杂任务](https://www.kimi.com/replay/19f6af1a-b402-8a62-8000-0000ed366eab)
- [Open Sea — Realtime WebGPU Ocean（原始演示链接）](https://avj2vp5rk3fqe.ok.kimi.link/?id=2077778172904054784&share_id=19f6af1a-b402-8a62-8000-0000ed366eab)

---

## ✨ 功能特性

| 特性 | 说明 |
|------|------|
| **🌊 程序化波浪** | 五方向 Gerstner 涌浪叠加，含解析法线/切线计算 |
| **🌅 动态天空** | 基于太阳位置的程序化光谱天空，含云层渲染 |
| **☀️ 时段切换** | 黄昏 → 黄金时段 → 午后 → 正午，颜色平滑插值 |
| **✨ 光照特效** | Fresnel 反射、Bloom 泛光、太阳闪烁、泡沫 |
| **🖥️ 自适应性能** | 5 级质量档位 + GPU 基准测试 + 实时 FPS 监控自动调节 |
| **🌐 中英文切换** | 一键切换界面语言 |

---

## 🏗️ 技术栈

| 层面 | 技术 | 版本 |
|------|------|------|
| **渲染后端** | WebGPU | — |
| **3D 框架** | Three.js (WebGPURenderer) | ^0.178.0 |
| **着色器** | Three.js TSL 节点着色器 | — |
| **后处理** | Three.js PostProcessing + BloomNode | — |
| **控制** | OrbitControls | — |
| **语言** | JavaScript (ES Module) | — |
| **构建** | 无构建步骤，纯 CDN 加载 | — |

### 浏览器要求

| 浏览器 | 最低版本 |
|--------|----------|
| Google Chrome | ≥ 113 |
| Microsoft Edge | ≥ 113 |
| Safari | ≥ 16.4 (macOS) |

> 需启用硬件加速。

---

## 🎮 交互说明

| 操作 | 功能 |
|------|------|
| **拖拽** | 旋转视角（OrbitControls） |
| **滚轮** | 缩放 |
| **Sea State 滑条** | 调节海况（0.25 → 1.75） |
| **Time of Day 滑条** | 调节时段（黄昏 ↔ 正午） |
| **Drift 按钮** | 开关自动旋转 |
| **Ultra HD 按钮** | 切换自适应 / 超高清模式 |
| **EN/中 按钮** | 切换界面语言 |

---

## ⚙️ 自适应质量系统

系统根据 GPU 型号、屏幕分辨率和实时帧率自动选择最佳画质。

### 1. 5 级质量档位
| 档位 | 像素比 | 网格细分 | 天空细分 | Bloom | FBM 层数 | 特效 |
|------|--------|---------|---------|-------|---------|------|
| **Ultra** (0) | min(DPR, 2) | 440×440 | 48×24 | 100% | 3 | 全部 |
| **High** (1) | min(DPR, 1.5) | 340×340 | 36×18 | 70% | 3 | 全部 |
| **Medium** (2) | min(DPR, 1.2) | 260×260 | 28×14 | 40% | 2 | 无泡沫 |
| **Low** (3) | min(DPR, 1.0) | 200×200 | 24×12 | 关闭 | 2 | 仅闪烁 |
| **Potato** (4) | min(DPR, 0.75) | 140×140 | 16×8 | 关闭 | 1 | 基础 |

### 2. 智能初始检测
- 通过 `navigator.gpu.requestAdapter()` 读取 GPU 供应商/架构信息
- Intel/Mali 等低端 GPU 自动降级，NVIDIA/Apple Silicon 等高端 GPU 自动升级
- 高分辨率屏幕（4K+）额外降级以减轻填充率压力

### 3. 实时 FPS 监控 + 自适应
- **降级**: 连续 FPS < 35 → 立即降一档（2 秒冷却）
- **升级**: FPS ≥ 58 稳定 2.5 秒 → 自动升一档（2.5 秒冷却）
- 渲染帧重建时自动释放旧几何体并创建新几何体

### 4. GPU 初始基准测试
- 启动时采集 20 帧进行性能评估
- 若低于阈值则继续降档重测，直到找到合适的档位

### 5. 着色器层面的优化
- `FBM` 函数改为通过 `uFbmOctaves` 制服变量控制层数，低配设备只计算 1-2 层噪声
- Bloom 强度按档位缩放，低配设备完全关闭
- 泡沫特效在中低档位跳过，减少 2 次 FBM 噪声采样

### 6. 响应式处理
- `resize` 时自动调用 `PERF.applyPixelRatio()` 确保像素比始终适配

---

## 🌊 渲染技术细节

### 波浪系统
- 5 个方向 Gerstner 波叠加（波长 5 → 60）
- 解析切平面累积 → 稳定的大尺度涌浪法线
- FBM 有限差分 → 毛细尺度微表面细节法线
- 波峰检测 → SSS 辉光 + 泡沫

### 天空系统
- 基于太阳方向的光谱散射（Zenith ↔ Horizon 插值）
- 接近地平线的程序化云层（FBM 噪声驱动）
- 太阳光晕 + 日面高光

### 海洋着色
- 水深渐变色（Deep ↔ Shallow）
- Fresnel 项驱动的天空反射
- 噪声调制的太阳闪烁（高光 lobe + 泛光 sheen）
- 波峰泡沫遮罩
- 视距渐隐（Horizon 混合）
